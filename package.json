{
  "name": "pyrolysis-scada",
  "version": "1.0.0",
  "description": "Pyrolysis Plant Virtual SCADA",
  "scripts": {
    "start": "node-red -u . -p $PORT"
  },
  "dependencies": {
    "node-red": "^3.1.0",
    "node-red-dashboard": "^3.6.0"S
  },
  "engines": {
    "node": ">=18.0.0"
  }
}