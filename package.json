{
  "name": "pyrolysis-scada",
  "version": "1.0.0",
  "description": "Pyrolysis Plant Virtual SCADA",
  "main": "node_modules/node-red/red.js",
  "scripts": {
    "start": "node-red --settings settings.js --userDir . -p $PORT"
  },
  "dependencies": {
    "node-red": "^3.1.0",
    "node-red-dashboard": "^3.6.0"
  },
  "engines": {
    "node": "18.x"
  }
}
